{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Chrome para localhost",
            "url": "http://localhost:8090/",
            "webRoot": "${workspaceFolder}"
        }
    ]
}